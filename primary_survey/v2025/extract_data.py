import configparser
import shutil

# This function copies the data from the data_extract_path to the local input directory for processing
def extract_data(logger):
    logger.warning("Extraction not necessary.")
