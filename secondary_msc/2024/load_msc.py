def load_births(logger):
    logger.warning("Loader for secondary data hasn't been written yet!")


def load_from_queries(logger):
    logger.warning("Loader for secondary data hasn't been written yet!")
